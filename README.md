# QuestBoard

A to-do app that turns your day into a procedural ASCII dungeon.
Completing quests unlocks rooms.

## Run locally

### 1) Setup
```bash
cd questboard
python3 -m venv .venv
source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
pip install -r requirements.txt
